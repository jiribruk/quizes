# ===========================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ===========================================
# Copy this file to .env and update values for production

# Database configuration
DATABASE=quizzes_production
POSTGRES_HOST=db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD
POSTGRES_PORT=5432

# Redis configuration
REDIS_URL=redis://redis:6379/0

# Application ports (for Raspberry Pi shared environment)
WEB_PORT=3001
NGINX_HTTP_PORT=8080
NGINX_HTTPS_PORT=8443
POSTGRES_EXTERNAL_PORT=5433
REDIS_EXTERNAL_PORT=6380

# Domain configuration
DOMAIN=smartiestapps.quizzes.com
BASE_DOMAIN=smartiestapps.com

# Rails configuration
RAILS_ENV=production
SECRET_KEY_BASE=CHANGE_THIS_GENERATE_WITH_RAILS_SECRET

# Application configuration
RAILS_SERVE_STATIC_FILES=true
RAILS_LOG_TO_STDOUT=true

# ===========================================
# PRODUCTION SETUP INSTRUCTIONS:
# ===========================================
# 1. Copy this file: cp env.production.example .env
# 2. Generate SECRET_KEY_BASE:
#    docker-compose exec web bundle exec rails secret
# 3. Update POSTGRES_PASSWORD with secure password
# 4. Update DATABASE name if needed
# 5. Set up SSL certificates for HTTPS
# 6. Configure proper firewall rules
# 7. Set up monitoring and backups
#
# SECURITY NOTES:
# - Never commit .env file to version control
# - Use strong passwords (min 16 characters)
# - Rotate SECRET_KEY_BASE regularly
# - Use environment-specific database names
# - Enable SSL/TLS encryption
# - Set up proper logging and monitoring
